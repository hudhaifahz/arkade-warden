# Arkade wallet extension

The Warden screen targets `arkade-os/wallet` commit
`a5d0ec09b366a78d0efead0bfd5bf2474fbf6965` under its MIT license.

Overlay the three files under `src/` onto the same paths in that checkout.
Review and rebuild the wallet rather than copying a prebuilt bundle.
