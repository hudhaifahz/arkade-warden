{
  "name": "frontier-crown-arkade-home-edge",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run --config wrangler.example.jsonc",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.130.0"
  }
}
