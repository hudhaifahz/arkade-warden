{
  "name": "@frontier-crown/arkade-community-edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types",
    "check": "wrangler types && tsc --noEmit && wrangler deploy --dry-run",
    "test": "tsx --test src/index.test.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "tsx": "4.20.5",
    "typescript": "5.9.2",
    "wrangler": "^4.130.0"
  }
}
